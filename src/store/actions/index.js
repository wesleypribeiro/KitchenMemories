import * as example from './example.actions';
import * as login from './user.actions';
import * as updateFavorites from './favoriteRecipes.actions';

const actions = {
  example,
  login,
  updateFavorites,
};

export default actions;
